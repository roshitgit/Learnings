1.	After dll is created, create a sample C# console app and reference the dll.
2.	Call methods exposed by the DLL API with the service now change ticket number to get desired results.
3.	Finally create .MSG outlook message format file with the results from api call and save it on NAS server using inpersonation.
