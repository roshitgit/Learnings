1. Any console job can be scheduled via "Autosys".
2. If the database server is using "Windows Auth", then the autosys FID should be added as a login into database with full permissions to 
execute the .exe file
